{
  "name": "carousel-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Offline carousel maker for Instagram and LinkedIn. The app itself has no dependencies; npm is only used to run the tests.",
  "scripts": {
    "test": "node --test tests/unit.test.js tests/e2e.test.js",
    "test:unit": "node --test tests/unit.test.js",
    "test:e2e": "node --test tests/e2e.test.js",
    "serve": "python3 -m http.server 8123"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
